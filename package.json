{
  "name": "contentcode",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:server": "bun --filter @contentcode/server dev",
    "start:server": "bun --filter @contentcode/server start",
    "dev:cli": "bun run --cwd apps/cli --silent dev",
    "start:cli": "bun run --cwd apps/cli --silent start",
    "check": "bun --filter '*' check"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
